# Run Analysis

Run: `2026-09-20-ready-success-v1-rps-sweep-5d45e21`

Results: [result.json](result.json)

This document is generated by AI from the run metadata and machine results.

## Purpose

Repeat the published ready-path sweep against the current harness and
lognormal execution model while holding topology and workload inputs constant.

## Observations

| Requested RPS | Achieved RPS | Worker RPS | Queue p95 | Delivery p95 | Drain |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 200 | 200.0 | 200.5 | 4.3 ms | 14.8 ms | 1 ms |
| 300 | 300.0 | 300.5 | 48.8 ms | 53.5 ms | 6 ms |
| 325 | 325.0 | 314.4 | 379.6 ms | 383.5 ms | 371 ms |
| 350 | 349.9 | 311.2 | 1182.9 ms | 1187.9 ms | 1.27 s |
| 400 | 400.0 | 310.5 | 2807.8 ms | 2811.3 ms | 2.92 s |
| 800 | 799.8 | 283.8 | 17368.8 ms | 17371.0 ms | 18.21 s |

- Producers sustained every requested arrival rate through 800 RPS.
- Worker throughput tracked arrival through 300 RPS and plateaued around 310
  to 314 tasks per second once requested load reached 325 RPS.
- Every point completed with zero missing, duplicate, outstanding, or
  dead-lettered workload IDs.
- Generated execution times averaged 4.86 to 4.90 ms, with p95 around 12.4 to
  13.0 ms, p99 around 19.0 to 20.9 ms, and maxima around 51 to 52 ms.
- The lognormal sampler therefore produced a real tail above the configured
  20 ms p99 rather than treating p99 as an absolute maximum.

## Comparison

The prior sweep at revision `82a19524a7c2` estimated the same 312 tasks per
second service ceiling, with 300 RPS sustainable and 325 RPS first overloaded.
The overload points are nearly unchanged:

| Requested RPS | Prior queue p95 | Current queue p95 |
| ---: | ---: | ---: |
| 300 | 87.7 ms | 48.8 ms |
| 325 | 377.2 ms | 379.6 ms |
| 350 | 1193.6 ms | 1182.9 ms |
| 400 | 2809.3 ms | 2807.8 ms |
| 800 | 17530.2 ms | 17368.8 ms |

The difference at 300 RPS is not enough to claim an improvement from one run,
while every overloaded point reproduces the prior backlog curve closely.

## Interpretation

- The current harness and lognormal sampler did not materially change the
  serial ready-path capacity result.
- The worker path remains the bottleneck; enqueue p95 stayed below 1.3 ms at
  every point while worker throughput stopped scaling above roughly 312 RPS.
- The highest practical operating point with headroom remains approximately
  250 RPS for this serial topology and execution distribution.

## Suggestions

1. Use this sweep as the serial baseline for the worker-concurrency stack.
2. Repeat selected points before interpreting the lower 300 RPS queue latency
   as a regression improvement.
3. Run the north-star sweep only after applying a topology with worker and
   producer concurrency sufficient for the 20k RPS target.

## Caveats

- Each point ran once on a local arm64 laptop with one Dockerized Kafka broker,
  one ready partition, two serial worker processes, and two producer processes.
- The workload uses a synthetic lognormal execution distribution rather than a
  production histogram.
- Absolute local numbers are diagnostic; the repeated saturation shape is the
  stronger result.
